# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: api\patchUser.spec.ts >> PATCH - Update only lastname
- Location: tests\api\patchUser.spec.ts:5:5

# Error details

```
Error: expect(received).toBe(expected) // Object.is equality

Expected: 200
Received: 503
```

# Test source

```ts
  1  | import { test, expect } from '@playwright/test';
  2  | import { UserApi } from '../../api/userApi';
  3  | 
  4  | // PATCH test - Update only some fields
  5  | test('PATCH - Update only lastname', async ({ request }) => {
  6  |   const userApi = new UserApi(request);
  7  | 
  8  |   // Step 1: Create a user
  9  |   const createResponse = await userApi.addUser(
  10 |     'Diana',
  11 |     'Prince',
  12 |     `diana${Date.now()}@test.com`,
  13 |     'password123'
  14 |   );
  15 |   
  16 |   const createBody = await createResponse.json();
  17 |   const userId = createBody.user._id;
  18 |   const token = createBody.token;
  19 |   const originalEmail = createBody.user.email;
  20 | 
  21 |   // Step 2: Update ONLY the lastName (PATCH - partial update)
  22 |   const patchResponse = await userApi.patchUser(
  23 |     userId,
  24 |     { lastName: 'Wonder' },  // Only change lastname
  25 |     token
  26 |   );
  27 | 
  28 |   // Step 3: Verify only lastname changed
> 29 |   expect(patchResponse.status()).toBe(200); // 200 = OK
     |                                  ^ Error: expect(received).toBe(expected) // Object.is equality
  30 |   
  31 |   const patchBody = await patchResponse.json();
  32 |   expect(patchBody.firstName).toBe('Diana');      // First name unchanged
  33 |   expect(patchBody.lastName).toBe('Wonder');      // Last name changed
  34 |   expect(patchBody.email).toBe(originalEmail);    // Email unchanged
  35 | });
  36 | 
```